import sdRDM

from typing import List, Optional
from pydantic import PrivateAttr, validator
from uuid import uuid4
from pydantic_xml import attr, element, wrapped
from sdRDM.base.listplus import ListPlus
from sdRDM.base.utils import forge_signature
from .datatypes import DataTypes
from .abstractspecies import AbstractSpecies


@forge_signature
class Replicate(
    sdRDM.DataModel,
    nsmap={
        "": "https://github.com/EnzymeML/enzymeml-specifications@142ca246cf92944bcbc11fbda9892f64bff77e8b#Replicate"
    },
):
    """This object contains the measured time course data as well as metadata to the replicate itself."""

    id: Optional[str] = attr(
        name="id",
        description="Unique identifier of the given object.",
        default_factory=lambda: str(uuid4()),
        xml="@id",
    )

    species_id: AbstractSpecies = element(
        description="Unique identifier of the species that has been measured.",
        tag="species_id",
        json_schema_extra=dict(),
    )

    measurement_id: str = element(
        description=(
            "Unique identifier of the measurement that the replicate is part of."
        ),
        tag="measurement_id",
        json_schema_extra=dict(),
    )

    data_type: DataTypes = element(
        description="Type of data that was measured (e.g. concentration)",
        default=DataTypes.CONCENTRATION,
        tag="data_type",
        json_schema_extra=dict(),
    )

    data_unit: str = element(
        description="SI unit of the data that was measured.",
        tag="data_unit",
        json_schema_extra=dict(),
    )

    time_unit: str = element(
        description="Time unit of the replicate.",
        tag="time_unit",
        json_schema_extra=dict(),
    )

    time: List[float] = wrapped(
        "time",
        element(
            description="Time steps of the replicate.",
            default_factory=ListPlus,
            tag="float",
            json_schema_extra=dict(multiple=True),
        ),
    )

    data: List[float] = wrapped(
        "data",
        element(
            description="Data that was measured.",
            default_factory=ListPlus,
            tag="float",
            json_schema_extra=dict(multiple=True),
        ),
    )

    is_calculated: bool = element(
        description="Whether or not the data has been generated by simulation.",
        default=False,
        tag="is_calculated",
        json_schema_extra=dict(),
    )

    uri: Optional[str] = element(
        description="URI of the protein.",
        default=None,
        tag="uri",
        json_schema_extra=dict(),
    )

    creator_id: Optional[str] = element(
        description="Unique identifier of the author.",
        default=None,
        tag="creator_id",
        json_schema_extra=dict(),
    )
    _repo: Optional[str] = PrivateAttr(
        default="https://github.com/EnzymeML/enzymeml-specifications"
    )
    _commit: Optional[str] = PrivateAttr(
        default="142ca246cf92944bcbc11fbda9892f64bff77e8b"
    )

    @validator("species_id")
    def get_species_id_reference(cls, value):
        """Extracts the ID from a given object to create a reference"""
        from .abstractspecies import AbstractSpecies

        if isinstance(value, AbstractSpecies):
            return value.id
        elif isinstance(value, str):
            return value
        else:
            raise TypeError(
                f"Expected types [AbstractSpecies, str] got '{type(value).__name__}'"
                " instead."
            )

    @validator("species_id")
    def get_species_id_reference(cls, value):
        """Extracts the ID from a given object to create a reference"""
        from .abstractspecies import AbstractSpecies

        if isinstance(value, AbstractSpecies):
            return value.id
        elif isinstance(value, str):
            return value
        else:
            raise TypeError(
                f"Expected types [AbstractSpecies, str] got '{type(value).__name__}'"
                " instead."
            )

    @validator("species_id")
    def get_species_id_reference(cls, value):
        """Extracts the ID from a given object to create a reference"""
        from .abstractspecies import AbstractSpecies

        if isinstance(value, AbstractSpecies):
            return value.id
        elif isinstance(value, str):
            return value
        else:
            raise TypeError(
                f"Expected types [AbstractSpecies, str] got '{type(value).__name__}'"
                " instead."
            )

    @validator("species_id")
    def get_species_id_reference(cls, value):
        """Extracts the ID from a given object to create a reference"""

        from .abstractspecies import AbstractSpecies

        if isinstance(value, AbstractSpecies):
            return value.id
        elif isinstance(value, str):
            return value
        else:
            raise TypeError(
                f"Expected types [AbstractSpecies, str] got '{type(value).__name__}'"
                " instead."
            )
